from pathlib import Path

CONFIG_ROOT_PATH = Path.home() / ".acm"
STORE_FILE_PATH = CONFIG_ROOT_PATH / "store"
BACKUP_FILE_PATH = CONFIG_ROOT_PATH / "previous"

AWS_CREDENTIALS_FILE_PATH = Path.home() / ".aws/credentials"
